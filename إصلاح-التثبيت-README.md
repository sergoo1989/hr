# 🔧 إصلاح نظام التثبيت على أجهزة العملاء

## ✅ المشكلة التي تم حلها

كان نظام التثبيت السابق يعاني من مشكلة رئيسية:
- **config.js** كان يستخدم `localhost` فقط
- عندما يفتح العميل النظام من جهاز آخر، كان يحاول الاتصال بـ localhost على جهازه (وليس السيرفر!)
- النتيجة: فشل الاتصال بـ API

## 🎯 الحل المطبق

### 1. تحديث config.js (الحل الذكي)
تم تعديل [config.js](frontend/config.js) ليكتشف IP السيرفر تلقائياً:

```javascript
getApiUrl: function() {
    const hostname = window.location.hostname;
    
    // إذا localhost، استخدم localhost
    if (hostname === 'localhost' || hostname === '127.0.0.1') {
        return 'http://localhost:3000';
    }
    
    // وإلا، استخدم نفس IP السيرفر
    return `http://${hostname}:3000`;
}
```

**النتيجة:**
- ✅ على السيرفر: يستخدم `http://localhost:3000`
- ✅ على العميل: يستخدم `http://192.168.1.100:3000` (نفس IP الـ Frontend)

### 2. تحسين ملفات التثبيت

#### [تثبيت-على-جهاز-العميل.bat](تثبيت-على-جهاز-العميل.bat)
- ✓ اختبار اتصال تلقائي بالسيرفر (ping test)
- ✓ رسائل خطأ واضحة
- ✓ واجهة أفضل مع رموز emoji
- ✓ معلومات مفصلة بعد التثبيت

#### [تحديث-اعدادات-العميل.bat](تحديث-اعدادات-العميل.bat)
- ✓ تحديث سهل لـ IP السيرفر
- ✓ اختبار اتصال قبل التحديث
- ✓ تحديث الاختصار تلقائياً

#### [إعداد-فايروول-السيرفر.bat](إعداد-فايروول-السيرفر.bat)
- ✓ إعداد Firewall تلقائياً
- ✓ فتح المنافذ 3000 و 8080
- ✓ يجب تشغيله كمسؤول

## 📋 دليل الاستخدام السريع

### على جهاز السيرفر:

```batch
# الخطوة 1: إعداد Firewall (مرة واحدة فقط)
إعداد-فايروول-السيرفر.bat  (Run as Administrator)

# الخطوة 2: تشغيل السيرفر
تشغيل-السيرفر.bat

# الخطوة 3: احفظ IP السيرفر (سيظهر تلقائياً)
مثال: 192.168.1.100
```

### على أجهزة العملاء:

```batch
# الخطوة 1: نسخ ملف التثبيت
تثبيت-على-جهاز-العميل.bat

# الخطوة 2: تشغيله وإدخال IP السيرفر
192.168.1.100

# الخطوة 3: فتح النظام من الاختصار
"نظام الموارد البشرية" (على سطح المكتب)
```

## 🧪 كيفية الاختبار

```batch
# اختبار جاهزية النظام
اختبار-النظام.bat
```

## 📁 الملفات المتاحة

### للسيرفر:
- ✅ `تشغيل-السيرفر.bat` - تشغيل النظام
- ✅ `إعداد-فايروول-السيرفر.bat` - إعداد Firewall

### للعملاء:
- ✅ `تثبيت-على-جهاز-العميل.bat` - التثبيت الأول
- ✅ `تحديث-اعدادات-العميل.bat` - تحديث IP

### للمساعدة:
- ✅ `دليل-تثبيت-العميل.md` - دليل شامل
- ✅ `دليل-سريع-للتثبيت.txt` - دليل مختصر
- ✅ `اختبار-النظام.bat` - اختبار الجاهزية

### الملفات الفنية:
- ✅ `frontend/config.js` - إعدادات API (محدّث)

## 🔍 استكشاف الأخطاء

### المشكلة: لا يمكن الوصول للسيرفر
**الحل:**
1. تأكد من تشغيل السيرفر (`تشغيل-السيرفر.bat`)
2. تحقق من Firewall (`إعداد-فايروول-السيرفر.bat`)
3. تأكد من أن الأجهزة على نفس الشبكة

### المشكلة: API لا يستجيب
**الحل:**
1. افتح Console في المتصفح (F12)
2. ابحث عن `API Configuration`
3. تأكد من أن API URL صحيح

### المشكلة: تغيّر IP السيرفر
**الحل:**
استخدم `تحديث-اعدادات-العميل.bat` على أجهزة العملاء

## 🎉 الميزات الجديدة

1. **اكتشاف تلقائي للسيرفر** - لا حاجة لملفات config منفصلة
2. **اختبار اتصال** - تحقق تلقائي من الوصول للسيرفر
3. **واجهة محسّنة** - رسائل واضحة ورموز بصرية
4. **معلومات مفصلة** - ملف معلومات-الاتصال.txt
5. **سهولة التحديث** - تحديث IP في خطوة واحدة
6. **اختبار جاهزية** - ملف اختبار-النظام.bat

## 📊 المتطلبات

### على السيرفر:
- ✓ Windows
- ✓ Node.js مثبت
- ✓ Python مثبت (لـ Frontend Server)
- ✓ Firewall يسمح بالمنافذ 3000 و 8080

### على العملاء:
- ✓ Windows
- ✓ متصفح حديث
- ✓ اتصال بنفس الشبكة المحلية

## 🔐 الأمان

- النظام مصمم للشبكة المحلية فقط
- لا يُنصح بتعريضه للإنترنت مباشرة
- غيّر كلمات المرور الافتراضية
- تأكد من تفعيل Firewall

## 📞 الدعم

للمزيد من المساعدة:
1. راجع [دليل-تثبيت-العميل.md](دليل-تثبيت-العميل.md)
2. راجع [دليل-سريع-للتثبيت.txt](دليل-سريع-للتثبيت.txt)
3. شغّل [اختبار-النظام.bat](اختبار-النظام.bat)

---

**تاريخ الإصلاح:** 14 ديسمبر 2025  
**الإصدار:** 2.0  
**الحالة:** ✅ جاهز للاستخدام
