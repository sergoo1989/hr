# 🏢 نظام إدارة الموارد البشرية والرواتب المتكامل
## HR & Payroll Management System - KSA Compliant

نظام متكامل لإدارة الموارد البشرية والرواتب متوافق 100% مع قانون العمل السعودي

---

## ✨ الميزات الرئيسية

### 📊 للموظفين (Employee Portal)
- ✅ لوحة معلومات شاملة مع:
  - رصيد الإجازات (أيام + قيمة نقدية)
  - حالة تذكرة السفر السنوية
  - بدل نهاية الخدمة المتوقع
  - التنبيهات المهمة (وثائق منتهية، سلف مستحقة)
- ✅ تقديم طلبات الإجازات والسلف
- ✅ عرض كشوف الرواتب
- ✅ متابعة الحضور والانصراف
- ✅ العهود والأصول المستلمة
- ✅ المستندات الشخصية

### 🔐 للإدارة (Admin Portal)
- ✅ لوحة تحكم إدارية متقدمة مع:
  - إحصائيات سريعة (موظفين، رواتب، إجازات)
  - رسوم بيانية (توزيع الإجازات، الحضور، الرواتب)
  - تنبيهات المستندات المنتهية
- ✅ إدارة كاملة للموظفين
- ✅ الموافقة على الإجازات والسلف
- ✅ إدارة العقود والحضور
- ✅ توليد كشوف الرواتب
- ✅ تقارير شاملة (رواتب، إجازات، امتثال)

### ⚖️ التوافق مع قانون العمل السعودي
- ✅ **الإجازات السنوية**: 21 يوم (أول 5 سنوات) → 30 يوم (بعد 5 سنوات)
- ✅ **الإجازات المرضية**: 
  - أول 30 يوم: 100% من الراتب
  - ثاني 60 يوم: 75% من الراتب
  - بعد 90 يوم: بدون راتب
- ✅ **بدل نهاية الخدمة**: 
  - أول 5 سنوات: نصف راتب لكل سنة
  - بعد 5 سنوات: راتب كامل لكل سنة
  - أجزاء الاستحقاق: 1/3, 2/3, كامل (حسب نوع الإنهاء)
- ✅ **تذاكر السفر**: تذكرة سنوية لكل موظف
- ✅ **سجل التدقيق**: تسجيل كامل لجميع العمليات الحساسة

---

## 🚀 البدء السريع

### المتطلبات
- Node.js v18+
- npm/yarn
- SQLite (للتطوير) أو PostgreSQL/MySQL (للإنتاج)

### التثبيت

```bash
# 1. استنساخ المشروع
git clone <repository-url>
cd hr

# 2. تثبيت المكتبات
npm install

# 3. إعداد قاعدة البيانات
cd packages/db
npx prisma db push
npx prisma studio  # لفتح واجهة قاعدة البيانات

# 4. تشغيل السيرفر
cd ../../apps/api
npm start
```

السيرفر يعمل الآن على: **http://localhost:4000**

---

## 📁 هيكل المشروع

```
hr/
├── apps/
│   └── api/                    # تطبيق NestJS API
│       ├── src/
│       │   ├── auth/           # نظام المصادقة (JWT + RBAC)
│       │   ├── employee/       # وحدة الموظفين
│       │   │   ├── employee.controller.ts
│       │   │   ├── employee.service.ts
│       │   │   └── employee-dashboard.service.ts  # لوحة معلومات الموظف
│       │   ├── admin/          # وحدة الإدارة
│       │   ├── dashboard/      # لوحات التحكم والتقارير
│       │   │   ├── dashboard.service.ts
│       │   │   └── admin-dashboard.service.ts     # لوحة تحكم الإدارة
│       │   ├── payroll/        # نظام الرواتب
│       │   ├── leave/          # إدارة الإجازات (KSA compliance)
│       │   ├── audit/          # سجل التدقيق
│       │   ├── termination/    # إنهاء الخدمة (EOSB)
│       │   ├── scheduler/      # جدولة المهام (فحص المستندات)
│       │   └── main.ts         # نقطة الدخول
│       └── package.json
│
├── packages/
│   └── db/                     # قاعدة البيانات (Prisma)
│       ├── schema.prisma       # نموذج قاعدة البيانات الكامل
│       └── dev.db              # قاعدة بيانات SQLite (تطوير)
│
├── API_DOCUMENTATION.md        # توثيق API الكامل
└── README.md                   # هذا الملف
```

---

## 🔌 API Endpoints

### للموظفين (`/employees/me/*`)
```
GET    /employees/me/dashboard          # لوحة المعلومات الكاملة
GET    /employees/me/leave-balance      # رصيد الإجازات
GET    /employees/me/travel-ticket      # حالة تذكرة السفر
POST   /employees/me/travel-ticket/use  # استخدام تذكرة السفر
GET    /employees/me/end-of-service     # بدل نهاية الخدمة المتوقع
GET    /employees/me/leaves             # قائمة الإجازات
POST   /employees/me/leaves             # تقديم طلب إجازة
POST   /employees/me/advances           # طلب سلفة
GET    /employees/me/attendance         # سجل الحضور
GET    /employees/me/assets             # العهود المستلمة
GET    /employees/me/documents          # المستندات
```

### للإدارة (`/dashboard/*` & `/admin/*`)
```
GET    /dashboard                       # لوحة التحكم الكاملة
GET    /dashboard/stats                 # إحصائيات سريعة
GET    /dashboard/charts                # بيانات الرسوم البيانية
GET    /dashboard/alerts/expiry         # تنبيهات المستندات
GET    /dashboard/reports/payroll       # تقرير الرواتب
GET    /dashboard/reports/leave         # تقرير الإجازات
GET    /admin/leaves/pending            # طلبات الإجازات المعلقة
PATCH  /admin/leaves/:id/approve        # الموافقة على إجازة
PATCH  /admin/leaves/:id/reject         # رفض إجازة
POST   /admin/contracts                 # إضافة عقد جديد
POST   /admin/attendance                # تسجيل حضور
POST   /admin/assets/assign             # تعيين عهدة
```

### نظام الجدولة (`/scheduler/*`)
```
GET    /scheduler/check-documents       # فحص يدوي للمستندات
GET    /scheduler/expiry-report         # تقرير المستندات المنتهية
```

**للتوثيق الكامل**: اطّلع على [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)

---

## 🗄️ قاعدة البيانات

### الجداول الرئيسية:
- **User** - مستخدمي النظام (EMPLOYEE/ADMIN)
- **Employee** - بيانات الموظفين الكاملة (إقامة، جواز، بنك، تأمين)
- **Contract** - عقود العمل (محدد/غير محدد، رواتب، بدلات)
- **Leave** - الإجازات (سنوية، مرضية، طارئة)
- **TravelTicket** - تذاكر السفر السنوية
- **Advance** - السلف
- **Asset** - العهود العينية
- **Attendance** - الحضور والانصراف
- **Document** - المستندات
- **Entitlement** - الاستحقاقات (نهاية الخدمة، إجازات)
- **AuditLog** - سجل التدقيق
- **PayrollRun** & **PayrollItem** - كشوف الرواتب
- **FinalSettlement** - التسوية النهائية

### عرض البيانات:
```bash
cd packages/db
npx prisma studio
```
يفتح واجهة رسومية على: **http://localhost:5555**

---

## 🔐 المصادقة والأدوار

### الأدوار:
- **EMPLOYEE**: الوصول إلى `/employees/me/*` فقط
- **ADMIN**: الوصول الكامل لجميع المسارات

### مثال تسجيل الدخول:
```bash
curl -X POST http://localhost:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "password123"
  }'
```

Response:
```json
{
  "access_token": "eyJhbGc...",
  "user": {
    "id": 1,
    "username": "admin",
    "role": "ADMIN"
  }
}
```

### استخدام Token:
```bash
curl -X GET http://localhost:4000/employees/me/dashboard \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

---

## 🧮 الخوارزميات الذكية

### 1. حساب رصيد الإجازات (`calculateLeaveBalance`)
```typescript
// يحسب:
// - الأيام المستحقة (حسب العقد)
// - الأيام المستخدمة (السنة الحالية)
// - الأيام المتبقية
// - القيمة النقدية (راتب يوم × أيام متبقية)
```

### 2. إدارة تذاكر السفر (`checkTravelTicketEligibility`)
```typescript
// ينشئ تلقائياً تذكرة سفر للسنة الحالية
// يتحقق من الاستخدام
// يسمح باستخدام مرة واحدة فقط سنوياً
```

### 3. حساب بدل نهاية الخدمة (`calculateEndOfService`)
```typescript
// يحسب حسب قانون العمل السعودي:
// - أقل من سنتين: لا يستحق
// - أول 5 سنوات: نصف راتب × سنوات
// - بعد 5 سنوات: راتب كامل × سنوات إضافية
// - يطبق الأجزاء (1/3, 2/3) حسب نوع الإنهاء
```

### 4. فحص المستندات المنتهية (`checkExpiringDocuments`)
```typescript
// يفحص يومياً:
// - الإقامات
// - جوازات السفر
// - رخص العمل
// - التأمين الطبي
// ينبّه قبل: 30, 60, 90 يوم
```

---

## 📊 التقارير المتوفرة

### 1. تقرير الرواتب
- إجمالي الرواتب الشهرية
- توزيع الرواتب حسب الفئات
- تفاصيل كل موظف (أساسي + بدلات - استقطاعات)
- حالة WPS

### 2. تقرير الإجازات
- توزيع حسب النوع (سنوية، مرضية، طارئة)
- توزيع حسب الحالة (موافق، معلق، مرفوض)
- إجمالي الأيام المستخدمة
- تفاصيل كل موظف

### 3. تقرير الامتثال
- المستندات المنتهية
- العقود المنتهية
- الموظفين الذين استحقوا زيادة أيام الإجازة
- تقرير بدلات نهاية الخدمة

---

## ⚠️ التنبيهات التلقائية

### فحص يومي (9 صباحاً) للمستندات:
- **حرج** (CRITICAL): أقل من 30 يوم
- **تحذير** (WARNING): 30-60 يوم
- **معلومة** (INFO): 60-90 يوم

### أنواع المستندات المراقبة:
- الإقامة
- جواز السفر
- رخصة العمل
- التأمين الطبي

---

## 🧪 الاختبار

### تشغيل Prisma Studio:
```bash
cd packages/db
npx prisma studio
```

### اختبار API باستخدام curl:
```bash
# 1. تسجيل الدخول
TOKEN=$(curl -s -X POST http://localhost:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"password123"}' \
  | jq -r '.access_token')

# 2. جلب لوحة المعلومات
curl -X GET http://localhost:4000/employees/me/dashboard \
  -H "Authorization: Bearer $TOKEN"

# 3. طلب إجازة
curl -X POST http://localhost:4000/employees/me/leaves \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "leaveType": "ANNUAL",
    "startDate": "2025-12-20",
    "endDate": "2025-12-25",
    "daysCount": 5,
    "paid": true
  }'
```

---

## 🔧 التكوين

### تغيير قاعدة البيانات:
في `packages/db/schema.prisma`:
```prisma
datasource db {
  provider = "postgresql"  // بدلاً من sqlite
  url      = env("DATABASE_URL")
}
```

### تغيير منفذ السيرفر:
في `apps/api/src/main.ts`:
```typescript
await app.listen(3000);  // بدلاً من 4000
```

### إضافة Cron Jobs:
في `apps/api/src/app.module.ts`:
```typescript
import { ScheduleModule } from '@nestjs/schedule';

@Module({
  imports: [
    ScheduleModule.forRoot(),  // تفعيل الجدولة
    // ... باقي الوحدات
  ]
})
```

---

## 📚 التقنيات المستخدمة

- **Backend**: NestJS 10
- **Database**: Prisma ORM (SQLite/PostgreSQL/MySQL)
- **Authentication**: JWT + Passport
- **Validation**: class-validator
- **Scheduling**: @nestjs/schedule
- **Logging**: Winston (مقترح)
- **Testing**: Jest (مقترح)

---

## 🛣️ خارطة الطريق

### المرحلة القادمة (Q1 2026):
- [ ] توليد ملفات WPS حسب مواصفات وزارة الموارد البشرية
- [ ] نظام الإشعارات (Email/SMS)
- [ ] استيراد الحضور من ملفات CSV
- [ ] تقارير Excel متقدمة
- [ ] تطبيق موبايل (React Native)
- [ ] واجهة مستخدم ويب (React/Vue.js)
- [ ] دمج مع أنظمة البصمة
- [ ] دمج مع الأنظمة البنكية
- [ ] نظام الأرشفة الإلكترونية

### المرحلة المستقبلية:
- [ ] ذكاء اصطناعي للتنبؤ بدوران الموظفين
- [ ] تحليلات متقدمة لأنماط الحضور
- [ ] نظام تقييم الأداء
- [ ] إدارة التدريب والتطوير
- [ ] نظام التوظيف (ATS)

---

## 🤝 المساهمة

المساهمات مرحب بها! يرجى:
1. Fork المشروع
2. إنشاء branch للميزة (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للـ branch (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

---

## 📝 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف [LICENSE](LICENSE) للتفاصيل

---

## 👨‍💻 المطور

تم تطويره بواسطة: **فريق تطوير نظام الموارد البشرية**  
التاريخ: 13 ديسمبر 2025  
الإصدار: 1.0.0

---

## 📞 الدعم

للدعم الفني أو الاستفسارات:
- 📧 Email: support@hr-system.com
- 📱 Phone: +966 XX XXX XXXX
- 🌐 Website: https://hr-system.com

---

<div dir="rtl">

## ملخص النظام

نظام متكامل لإدارة الموارد البشرية يجمع بين:
- ✅ **الامتثال الكامل** لقانون العمل السعودي
- ✅ **واجهات سهلة** للموظفين والإدارة
- ✅ **أتمتة ذكية** للعمليات المتكررة
- ✅ **تقارير شاملة** للقرارات الإدارية
- ✅ **قابلية التوسع** والتكامل مع أنظمة أخرى

</div>

---

**جاهز للاستخدام الفوري! 🚀**
