# 🚀 دليل سريع - نظام تفعيل حسابات الموظفين

## ⚡ البدء السريع | Quick Start

### 1. إعداد البريد الإلكتروني (إلزامي):

أنشئ ملف `.env` في مجلد `apps/api/`:

```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
```

**للحصول على كلمة مرور Gmail:**
1. افتح: https://myaccount.google.com/apppasswords
2. فعّل "التحقق بخطوتين" إذا لم يكن مفعلاً
3. أنشئ "كلمة مرور تطبيق" جديدة
4. انسخها واستخدمها في `EMAIL_PASSWORD`

### 2. تشغيل النظام:

```bash
cd apps/api
npm run start:dev
```

### 3. اختبار النظام:

1. **افتح لوحة التحكم**: http://localhost:3000/frontend/admin-dashboard.html
2. **سجل دخول كمدير**:
   - Username: `admin`
   - Password: `admin123`
3. **أضف موظف جديد**:
   - املأ جميع البيانات
   - **مهم جداً**: أضف بريد إلكتروني صحيح
   - اضغط "حفظ"
4. **تحقق من Console**:
   ```
   ✅ تم إرسال بريد التفعيل إلى: employee@example.com
   👤 اسم المستخدم: employee.name
   🔑 كلمة المرور المؤقتة: Abc123XyZ9
   ```
5. **افتح البريد الإلكتروني**:
   - ستجد بريد تفعيل احترافي
   - انقر على زر "تفعيل الحساب"
6. **سجل دخول بالبيانات المؤقتة**
7. **سيتم توجيهك تلقائياً لتغيير كلمة المرور**
8. **أدخل كلمة مرور جديدة قوية**
9. **✅ تم! الموظف يستطيع استخدام النظام**

---

## 📋 ما تم إضافته؟ | What's New?

### ✅ نظام تفعيل الحساب عبر البريد
- إرسال بريد تلقائي عند إضافة موظف
- اسم مستخدم يُنشأ تلقائياً
- كلمة مرور مؤقتة آمنة

### ✅ تغيير كلمة المرور الإلزامي
- صفحة مخصصة لتغيير كلمة المرور
- التحقق من قوة كلمة المرور
- منع استخدام نفس كلمة المرور القديمة

### ✅ بريد إلكتروني احترافي
- تصميم HTML جميل بالعربية
- يحتوي على جميع التفاصيل
- خطوات واضحة للموظف

---

## 🎯 الملفات المهمة | Important Files

### Backend:
- `apps/api/src/email/email.service.ts` - خدمة البريد الإلكتروني
- `apps/api/src/auth/auth.service.ts` - تغيير كلمة المرور
- `apps/api/src/employee/employee.service.ts` - إنشاء الحساب

### Frontend:
- `frontend/change-password.html` - صفحة تغيير كلمة المرور
- `frontend/login.html` - تسجيل الدخول (محدّث)

### Configuration:
- `apps/api/.env` - إعدادات البريد الإلكتروني
- `apps/api/.env.example` - مثال على الإعدادات

### Documentation:
- `EMAIL_ACTIVATION_SYSTEM.md` - الوثائق الكاملة

---

## ⚠️ ملاحظات مهمة | Important Notes

### 1. البريد الإلكتروني ضروري:
- لن يُرسل بريد إذا لم يكن الموظف لديه email
- لكن سيُنشأ الحساب ويُطبع في Console

### 2. كلمة المرور المؤقتة:
- يتم طباعتها في Console للطوارئ
- **لا تشاركها مع أحد**
- الموظف يجب أن يغيرها فوراً

### 3. إعادة تهيئة البيانات:
إذا أضفت موظفين قبل هذا التحديث، احذف البيانات القديمة:
```bash
rm data/hr-database.json
npm run start:dev
```

### 4. للإنتاج (Production):
- استخدم خدمة بريد احترافية (SendGrid, AWS SES)
- لا تستخدم Gmail في Production
- احفظ `.env` في سيرفر آمن

---

## 🐛 حل المشاكل | Troubleshooting

### البريد لا يُرسل؟
```
1. تحقق من .env
2. تأكد من كلمة مرور التطبيق صحيحة
3. جرب إرسال بريد اختبار يدوياً
4. تحقق من console للأخطاء
```

### لا يظهر صفحة تغيير كلمة المرور؟
```
1. تحقق من استجابة /auth/login
2. تأكد من أن data.mustChangePassword = true
3. احذف hr-database.json وأعد التشغيل
```

### TypeScript Errors؟
```bash
# احذف البيانات القديمة
rm data/hr-database.json
npm run start:dev
```

---

## 📞 للمساعدة | Help

إذا واجهت مشكلة، راجع:
1. هذا الملف (README السريع)
2. `EMAIL_ACTIVATION_SYSTEM.md` (الوثائق الكاملة)
3. Console logs للأخطاء

---

## ✅ قائمة التحقق | Checklist

قبل البدء، تأكد من:
- [ ] إنشاء ملف `.env` بإعدادات البريد
- [ ] الحصول على كلمة مرور تطبيق Gmail
- [ ] تشغيل السيرفر بنجاح
- [ ] لا توجد أخطاء في Console
- [ ] اختبار إضافة موظف ببريد حقيقي

---

**النظام جاهز! 🎉**

للتفاصيل الكاملة: [EMAIL_ACTIVATION_SYSTEM.md](EMAIL_ACTIVATION_SYSTEM.md)
