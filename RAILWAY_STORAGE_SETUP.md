# 🚨 إعداد التخزين الدائم على Railway

## ⚠️ مشكلة مهمة: Vercel لا يحفظ البيانات!

**Vercel يستخدم نظام ملفات مؤقت (Ephemeral Filesystem)**
- ❌ أي ملفات تُنشأ أثناء التشغيل تُحذف عند كل deployment جديد
- ❌ نظام التخزين المحلي (hr-database.json) **لن يعمل على Vercel**
- ❌ البيانات المدخلة ستُفقد عند كل تحديث

## ✅ الحل: استخدام Railway للـ API

Railway يدعم **Volumes** - تخزين دائم للملفات!

---

## 📋 خطوات إعداد Railway مع Volume

### الخطوة 1: إنشاء مشروع على Railway
1. اذهب إلى [railway.app](https://railway.app)
2. أنشئ مشروع جديد واربطه بـ GitHub repo
3. حدد **Root Directory**: `apps/api`

### الخطوة 2: إضافة Volume للتخزين الدائم ⭐

هذه الخطوة الأهم لحفظ البيانات!

1. في لوحة تحكم Railway، اضغط على service الخاص بك
2. اذهب إلى قسم **Settings**
3. مرر للأسفل حتى تجد **Volumes**
4. اضغط **"+ Add Volume"**
5. أدخل الإعدادات التالية:

```
Mount Path: /app/data
Name: hr-data (أو أي اسم تريده)
```

6. اضغط **Save**

### الخطوة 3: إضافة متغيرات البيئة

اذهب إلى **Variables** وأضف:

```bash
NODE_ENV=production
JWT_SECRET=your-super-secret-key-change-this-min-32-chars
JWT_EXPIRES_IN=7d
PORT=3000

# هذا المتغير سيُضاف تلقائياً من Railway عند إنشاء Volume
# RAILWAY_VOLUME_MOUNT_PATH=/app/data
```

### الخطوة 4: إعادة النشر

1. اضغط على **Deployments**
2. اضغط **"Redeploy"** على آخر deployment
3. انتظر اكتمال النشر

---

## 🔍 التحقق من عمل التخزين الدائم

### من Logs في Railway:
يجب أن ترى هذه الرسائل عند بدء التشغيل:

```
🔧 بيئة التشغيل: إنتاج
🔧 Railway: نعم
📁 مسار البيانات: /app/data
📄 ملف البيانات: /app/data/hr-database.json
📁 تم إنشاء مجلد البيانات (أو) 📂 تم تحميل البيانات من الملف
```

### اختبار الحفظ:
1. أضف موظف جديد من الواجهة
2. أعد نشر التطبيق (Redeploy)
3. ✅ الموظف الجديد يجب أن يكون موجوداً!

---

## 🏗️ البنية النهائية

```
┌─────────────────────────────────────────────────────────────┐
│                    المستخدم (Browser)                        │
└─────────────────────┬───────────────────────────────────────┘
                      │
         ┌────────────┴────────────┐
         │                         │
         ▼                         ▼
┌─────────────────┐      ┌─────────────────────┐
│     Vercel      │      │      Railway        │
│   (Frontend)    │      │      (API)          │
│                 │      │                     │
│ - HTML/CSS/JS   │      │ - NestJS Server     │
│ - Static Files  │      │ - Business Logic    │
│ - لا يحتاج      │      │                     │
│   تخزين بيانات  │      │ ┌─────────────────┐ │
│                 │      │ │    Volume       │ │
│                 │      │ │  /app/data      │ │
│                 │      │ │                 │ │
│                 │      │ │ hr-database.json│ │
│                 │      │ │ ✅ محفوظ دائماً │ │
│                 │      │ └─────────────────┘ │
└─────────────────┘      └─────────────────────┘
```

---

## ❓ أسئلة شائعة

### س: ماذا لو لم أضف Volume؟
**ج:** البيانات ستُفقد عند كل deployment جديد (نفس مشكلة Vercel)

### س: هل يمكن استخدام قاعدة بيانات خارجية؟
**ج:** نعم! يمكنك استخدام:
- Railway PostgreSQL
- PlanetScale (MySQL)
- MongoDB Atlas
- Supabase

لكن هذا يتطلب تعديل الكود لاستخدام Prisma أو TypeORM.

### س: ما حجم البيانات المدعوم في Volume؟
**ج:** يعتمد على خطتك في Railway. الخطة المجانية تدعم 1GB.

### س: هل البيانات آمنة؟
**ج:** نعم. Railway يستخدم تشفير للـ Volumes وتتم نسخها احتياطياً.

---

## 🔧 حلول بديلة

إذا كنت لا تريد استخدام Railway، يمكنك:

1. **Render.com** - يدعم Persistent Disk
2. **DigitalOcean App Platform** - يدعم Volumes
3. **VPS (مثل Hetzner, Linode)** - تحكم كامل
4. **Fly.io** - يدعم Volumes

---

## 📞 الدعم

إذا واجهت مشاكل:
1. تحقق من Logs في Railway
2. تأكد من أن Volume مُضاف ومربوط بـ `/app/data`
3. تأكد من وجود متغير `NODE_ENV=production`
