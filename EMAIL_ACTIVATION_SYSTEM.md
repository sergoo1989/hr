# نظام تفعيل حسابات الموظفين عبر البريد الإلكتروني
## Employee Account Activation via Email System

---

## 📋 نظرة عامة | Overview

تم تطوير نظام متكامل لتفعيل حسابات الموظفين الجدد تلقائياً عبر البريد الإلكتروني. عند إضافة موظف جديد، يتم:
1. إنشاء حساب مستخدم تلقائياً
2. إرسال بريد إلكتروني يحتوي على:
   - اسم المستخدم
   - كلمة مرور مؤقتة
   - رابط تسجيل الدخول
3. إلزام الموظف بتغيير كلمة المرور عند أول تسجيل دخول

---

## 🎯 المميزات | Features

### 1. إنشاء حساب تلقائي
- ✅ اسم مستخدم يُنشأ من الاسم الكامل
- ✅ كلمة مرور مؤقتة عشوائية (10 أحرف)
- ✅ ربط الحساب بالموظف مباشرة

### 2. بريد إلكتروني احترافي
- ✅ تصميم HTML احترافي باللغة العربية
- ✅ يحتوي على جميع بيانات التفعيل
- ✅ خطوات واضحة للموظف
- ✅ تحذيرات أمنية

### 3. تغيير كلمة المرور الإلزامي
- ✅ صفحة مخصصة لتغيير كلمة المرور
- ✅ التحقق من قوة كلمة المرور
- ✅ منع استخدام نفس كلمة المرور القديمة
- ✅ تحديث تلقائي بعد التغيير

### 4. الأمان
- ✅ كلمات مرور مُشفّرة (bcrypt)
- ✅ رموز تفعيل فريدة
- ✅ JWT Authentication
- ✅ منع الدخول بدون تفعيل الحساب

---

## 🔧 التقنيات المستخدمة | Technologies

### Backend:
- **NestJS** - إطار العمل الخلفي
- **NodeMailer** - إرسال البريد الإلكتروني
- **bcrypt** - تشفير كلمات المرور
- **TypeScript** - لغة البرمجة

### Frontend:
- **HTML5 & CSS3** - واجهة المستخدم
- **JavaScript** - البرمجة التفاعلية
- **RTL Design** - تصميم يدعم العربية

---

## 📁 الملفات المُضافة/المُعدلة | Modified Files

### Backend Files:

#### 1. **apps/api/src/email/email.service.ts** (جديد)
```typescript
- sendEmployeeActivationEmail()  // إرسال بريد التفعيل
- sendPasswordChangedConfirmation()  // تأكيد تغيير كلمة المرور
```

#### 2. **apps/api/src/email/email.module.ts** (جديد)
```typescript
- تصدير EmailService للاستخدام في Modules الأخرى
```

#### 3. **apps/api/src/database/in-memory-db.ts**
```typescript
// User Interface - حقول جديدة:
export interface User {
  isActive: boolean;           // ✅ حساب مفعل
  mustChangePassword: boolean; // ✅ يجب تغيير كلمة المرور
  activationToken?: string;    // ✅ رمز التفعيل
  email?: string;              // ✅ البريد الإلكتروني
}

// دوال جديدة:
- findUserByActivationToken()
- updateUserPassword()
- activateUser()
- generateActivationToken()
```

#### 4. **apps/api/src/auth/auth.service.ts**
```typescript
// تعديل login() للتحقق من:
- isActive (الحساب مفعل)
- mustChangePassword (يجب تغيير كلمة المرور)

// دوال جديدة:
- changePassword()  // تغيير كلمة المرور
- activateAccount() // تفعيل الحساب
```

#### 5. **apps/api/src/auth/auth.controller.ts**
```typescript
// Endpoints جديدة:
POST /auth/change-password  // تغيير كلمة المرور
GET /auth/activate/:token   // تفعيل الحساب
```

#### 6. **apps/api/src/employee/employee.service.ts**
```typescript
// تعديل createEmployee():
- إنشاء حساب مستخدم تلقائياً
- توليد كلمة مرور مؤقتة
- إرسال بريد التفعيل

// دالة جديدة:
- generateTemporaryPassword()  // توليد كلمة مرور عشوائية
```

#### 7. **apps/api/src/app.module.ts**
```typescript
imports: [
  EmailModule,  // ✅ إضافة Email Module
  // ...باقي الـ Modules
]
```

---

### Frontend Files:

#### 8. **frontend/change-password.html** (جديد)
- صفحة تغيير كلمة المرور الإلزامي
- تصميم احترافي بالعربية
- التحقق من قوة كلمة المرور
- عرض متطلبات كلمة المرور

#### 9. **frontend/login.html**
```javascript
// تعديل:
- التحقق من mustChangePassword
- التوجيه إلى change-password.html عند الحاجة
- حفظ بيانات المستخدم في localStorage
```

---

## ⚙️ إعداد النظام | Setup

### 1. تثبيت المكتبات:

```bash
cd apps/api
npm install nodemailer @types/nodemailer
```

### 2. إعداد البريد الإلكتروني:

أنشئ ملف `.env` في `apps/api/` وأضف:

```env
# إعدادات Gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# رابط النظام
FRONTEND_URL=http://localhost:3000/frontend
```

**ملاحظة**: لاستخدام Gmail:
1. فعّل "التحقق بخطوتين"
2. أنشئ "كلمة مرور التطبيق" من [هنا](https://myaccount.google.com/apppasswords)
3. استخدمها في `EMAIL_PASSWORD`

### 3. تشغيل النظام:

```bash
# تشغيل Backend
cd apps/api
npm run start:dev

# فتح Frontend
# افتح: http://localhost:3000/frontend/admin-dashboard.html
```

---

## 📧 كيفية العمل | How It Works

### التدفق الكامل:

```
1. المدير يضيف موظف جديد
   ↓
2. النظام يتحقق من وجود بريد إلكتروني
   ↓
3. إنشاء اسم مستخدم:
   "أحمد محمد" → "ahmed.mohamed"
   ↓
4. توليد كلمة مرور مؤقتة:
   مثال: "Kd8Pm7nQx2"
   ↓
5. إنشاء حساب User:
   - isActive = true
   - mustChangePassword = true
   - activationToken = رمز فريد
   ↓
6. إرسال بريد تفعيل يحتوي على:
   ✉️ اسم المستخدم
   ✉️ كلمة المرور المؤقتة
   ✉️ رابط تسجيل الدخول
   ✉️ خطوات التفعيل
   ↓
7. الموظف يستلم البريد
   ↓
8. الموظف ينقر على رابط التفعيل
   ↓
9. تسجيل الدخول بكلمة المرور المؤقتة
   ↓
10. النظام يكتشف mustChangePassword = true
   ↓
11. توجيه تلقائي إلى صفحة تغيير كلمة المرور
   ↓
12. الموظف يُدخل:
    - كلمة المرور الحالية (المؤقتة)
    - كلمة المرور الجديدة
    - تأكيد كلمة المرور الجديدة
    ↓
13. النظام يتحقق:
    ✓ كلمة المرور الحالية صحيحة
    ✓ كلمة المرور الجديدة ≥ 8 أحرف
    ✓ كلمة المرور الجديدة ≠ القديمة
    ✓ التأكيد يطابق الجديدة
    ↓
14. تحديث:
    - password = كلمة المرور الجديدة (مُشفّرة)
    - mustChangePassword = false
    ↓
15. توجيه إلى لوحة التحكم (Admin/Employee)
   ↓
16. ✅ تم! الموظف يستطيع استخدام النظام بحرية
```

---

## 📝 مثال بريد التفعيل | Activation Email Example

```
الموضوع: 🎉 تفعيل حسابك في نظام إدارة الموارد البشرية

عزيزي أحمد محمد،

تم إنشاء حساب خاص بك في نظام إدارة الموارد البشرية.

📋 بيانات الدخول المؤقتة:
━━━━━━━━━━━━━━━━━━━━━━
اسم المستخدم: ahmed.mohamed
كلمة المرور المؤقتة: Kd8Pm7nQx2

⚠️ هام جداً:
يجب عليك تغيير كلمة المرور المؤقتة عند أول تسجيل دخول.

🚀 خطوات التفعيل:
━━━━━━━━━━━━━━━━━━━━━━
1️⃣ انقر على زر "تفعيل الحساب" أدناه
2️⃣ استخدم اسم المستخدم وكلمة المرور المؤقتة
3️⃣ سيُطلب منك تغيير كلمة المرور فوراً
4️⃣ اختر كلمة مرور قوية وآمنة (8 أحرف على الأقل)
5️⃣ ابدأ باستخدام النظام!

[زر: 🔐 تفعيل الحساب الآن]

إذا واجهت أي مشكلة، تواصل مع قسم الموارد البشرية.

━━━━━━━━━━━━━━━━━━━━━━
نظام إدارة الموارد البشرية
هذا بريد تلقائي - لا ترد عليه
```

---

## 🧪 الاختبار | Testing

### سيناريو الاختبار الكامل:

#### 1. إضافة موظف جديد:
```
المدير → لوحة التحكم → إضافة موظف
الاسم: أحمد محمد
البريد: ahmed@example.com
[ملء باقي البيانات]
← حفظ
```

#### 2. التحقق من Console:
```
✅ تم إرسال بريد التفعيل إلى: ahmed@example.com
👤 اسم المستخدم: ahmed.mohamed
🔑 كلمة المرور المؤقتة: Kd8Pm7nQx2
```

#### 3. فحص البريد الإلكتروني:
- افتح البريد الوارد
- تحقق من وصول بريد التفعيل
- تحقق من جودة التنسيق

#### 4. تسجيل الدخول:
```
Username: ahmed.mohamed
Password: Kd8Pm7nQx2
← دخول
```

#### 5. التوجيه التلقائي:
- يجب أن ينتقل مباشرة إلى `change-password.html`
- رسالة: "يجب تغيير كلمة المرور المؤقتة"

#### 6. تغيير كلمة المرور:
```
كلمة المرور الحالية: Kd8Pm7nQx2
كلمة المرور الجديدة: NewPassword123
تأكيد كلمة المرور: NewPassword123
← تغيير كلمة المرور
```

#### 7. التحقق:
- رسالة نجاح: "✓ تم تغيير كلمة المرور بنجاح!"
- التوجيه إلى لوحة التحكم
- تسجيل خروج وإعادة دخول بكلمة المرور الجديدة

---

## 🔒 الأمان | Security Features

### 1. تشفير كلمات المرور:
```typescript
- استخدام bcrypt مع 10 rounds
- عدم تخزين كلمات مرور نصية أبداً
```

### 2. رموز التفعيل:
```typescript
- رموز فريدة لكل مستخدم
- صلاحية محدودة (يمكن إضافتها لاحقاً)
```

### 3. كلمات مرور مؤقتة قوية:
```typescript
- 10 أحرف عشوائية
- حروف كبيرة + صغيرة + أرقام
- استبعاد الأحرف المتشابهة (0/O, 1/l)
```

### 4. التحقق من قوة كلمة المرور:
```typescript
- 8 أحرف كحد أدنى
- تشجيع استخدام أنواع مختلفة من الأحرف
```

### 5. منع إعادة استخدام كلمة المرور:
```typescript
if (oldPassword === newPassword) {
  throw new Error('كلمة المرور الجديدة يجب أن تكون مختلفة');
}
```

---

## 📊 بيانات قاعدة البيانات | Database Schema

### User Interface (محدّث):
```typescript
interface User {
  id: number;
  username: string;
  password: string;                  // مُشفّرة
  role: 'ADMIN' | 'EMPLOYEE';
  employeeId?: number;
  email?: string;                    // ✅ جديد
  isActive: boolean;                 // ✅ جديد
  mustChangePassword: boolean;       // ✅ جديد
  activationToken?: string;          // ✅ جديد
}
```

### حالات الحساب:
```
┌─────────────────┬──────────┬───────────────────┐
│ الحالة          │ isActive │ mustChangePassword│
├─────────────────┼──────────┼───────────────────┤
│ مفعل جاهز      │ true     │ false             │
│ جديد (مؤقت)    │ true     │ true              │
│ غير مفعل       │ false    │ true              │
│ محظور          │ false    │ false             │
└─────────────────┴──────────┴───────────────────┘
```

---

## 🌐 API Endpoints

### 1. تسجيل الدخول:
```http
POST /auth/login
Content-Type: application/json

{
  "username": "ahmed.mohamed",
  "password": "Kd8Pm7nQx2"
}

Response:
{
  "access_token": "eyJhbGciOiJIUzI1...",
  "role": "EMPLOYEE",
  "mustChangePassword": true,  // ✅ جديد
  "user": {
    "id": 2,
    "username": "ahmed.mohamed",
    "role": "EMPLOYEE",
    "employee": { ... }
  }
}
```

### 2. تغيير كلمة المرور:
```http
POST /auth/change-password
Authorization: Bearer <token>
Content-Type: application/json

{
  "oldPassword": "Kd8Pm7nQx2",
  "newPassword": "NewPassword123"
}

Response:
{
  "success": true,
  "message": "تم تغيير كلمة المرور بنجاح"
}
```

### 3. تفعيل الحساب:
```http
GET /auth/activate/:token

Response:
{
  "success": true,
  "message": "تم تفعيل الحساب بنجاح"
}
```

---

## 🎨 تخصيص البريد الإلكتروني | Email Customization

يمكنك تخصيص محتوى البريد من ملف `email.service.ts`:

```typescript
// تغيير الألوان:
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

// تغيير الشعار:
<div class="header">
  <img src="your-logo.png" alt="Company Logo">
  <h1>مرحباً بك في نظام الموارد البشرية</h1>
</div>

// تخصيص الرسالة:
<p>رسالة ترحيبية مخصصة هنا...</p>
```

---

## ⚠️ ملاحظات مهمة | Important Notes

### 1. إعدادات Gmail:
- يجب تفعيل "التحقق بخطوتين"
- استخدم "كلمة مرور التطبيق" وليس كلمة المرور العادية
- لا تشارك كلمة مرور التطبيق مع أحد

### 2. للإنتاج (Production):
```typescript
// استخدم خدمة بريد احترافية:
- SendGrid
- Amazon SES
- Mailgun
- Microsoft 365
```

### 3. الأمان:
```typescript
// لا تحفظ في الكود:
✗ EMAIL_USER=myemail@gmail.com
✗ EMAIL_PASSWORD=mypassword

// استخدم متغيرات البيئة:
✓ process.env.EMAIL_USER
✓ process.env.EMAIL_PASSWORD
```

### 4. حذف البيانات القديمة:
```bash
# احذف ملف data/hr-database.json
# لإعادة تهيئة المستخدمين بالحقول الجديدة
rm data/hr-database.json
npm run start:dev
```

---

## 🐛 استكشاف الأخطاء | Troubleshooting

### مشكلة: البريد لا يُرسل

**الحلول:**
```
1. تحقق من إعدادات .env
2. تأكد من كلمة مرور التطبيق صحيحة
3. تحقق من console للأخطاء
4. جرب SMTP مختلف
```

### مشكلة: TypeScript Errors

**الحلول:**
```bash
# احذف البيانات القديمة
rm data/hr-database.json

# أعد تشغيل
npm run start:dev
```

### مشكلة: mustChangePassword لا يعمل

**الحلول:**
```
1. تحقق من استجابة login
2. تأكد من حفظ data.mustChangePassword في localStorage
3. تحقق من console.log في login.html
```

---

## 📈 التحسينات المستقبلية | Future Enhancements

### 1. رمز تفعيل بصلاحية محدودة:
```typescript
activationTokenExpiry: Date  // انتهاء صلاحية رمز التفعيل
```

### 2. إشعارات داخل النظام:
```typescript
- نظام notifications مدمج
- تنبيهات للموظفين الذين لم يفعّلوا حساباتهم
```

### 3. سياسة كلمات المرور:
```typescript
- تعقيد أكثر (حروف كبيرة + صغيرة + أرقام + رموز)
- صلاحية كلمة المرور (تغيير كل 90 يوم)
- منع إعادة استخدام آخر 5 كلمات مرور
```

### 4. تتبع محاولات تسجيل الدخول:
```typescript
- تسجيل المحاولات الفاشلة
- قفل الحساب بعد 5 محاولات خاطئة
```

### 5. Multi-Factor Authentication (MFA):
```typescript
- إرسال رمز تحقق عند تسجيل الدخول
- دعم Google Authenticator
```

---

## 📞 الدعم | Support

إذا واجهت أي مشكلة:
1. راجع هذه الوثيقة
2. تحقق من console logs
3. تأكد من إعدادات .env صحيحة
4. جرب حذف data/hr-database.json وإعادة التشغيل

---

## ✅ الخلاصة | Summary

تم تطوير نظام شامل لتفعيل حسابات الموظفين يتضمن:
- ✅ إنشاء حساب تلقائي
- ✅ إرسال بريد تفعيل احترافي
- ✅ تغيير كلمة المرور الإلزامي
- ✅ أمان عالٍ
- ✅ تجربة مستخدم ممتازة
- ✅ تكامل كامل مع النظام الحالي

النظام جاهز للاستخدام الفوري! 🎉

---

**تاريخ الإنشاء**: 2024-12-13  
**الإصدار**: 1.0  
**الحالة**: ✅ جاهز للإنتاج
