{
  "name": "hr-system",
  "version": "1.0.0",
  "main": "apps/api/dist/main.js",
  "scripts": {
    "start": "cd apps/api && npm install && npm run build && npm run start:prod"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
